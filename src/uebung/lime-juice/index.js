// @ts-check
//
// Die obige Zeile aktiviert die Typüberprüfung für diese Datei. Verschiedene IDEs interpretieren
// die @ts-check-Direktive. Sie gibt dir eine hilfreiche Autovervollständigung, wenn du
// Umsetzung dieser Übung.

/**
 * Bestimmt, wie lange es dauert, einen bestimmten Saft vorzubereiten.
 *
 * @param {string} name
 * @returns {number} Zeit in Minuten
 */
export function timeToMixJuice(name) {
  throw new Error("Bitte implementiere die Funktion timeToMixJuice");
}

/**
 * Berechnet die Anzahl der Limetten, die geschnitten werden müssen
 * um einen bestimmten Vorrat zu erreichen.
 *
 * @param {Anzahl} wedgesNeededed
 * @param {string[]} limes
 * @returns {number} Anzahl der geschnittenen Limetten
 */
export function limesToCut(wedgesNeeded, limes) {
  throw new Error("Bitte implementiere die Funktion limesToCut");
}

/**
 * Ermittelt, welche Säfte nach dem Ende der Schicht noch zubereitet werden müssen.
 *
 * @param {Zahl} timeLeft
 * @param {string[]} orders
 * @returns {string[]} verbleibende Aufträge, nachdem die Zeit abgelaufen ist
 */
export function remainingOrders(timeLeft, orders) {
  throw new Error("Bitte implementiere die Funktion remainingOrders");
}
